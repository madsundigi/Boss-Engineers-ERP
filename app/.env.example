# Copy to .env. The application reads tenant context (company/branch) and the
# authenticated user from the API gateway in production; in dev these are headers.
PORT=3001
NODE_ENV=development
DATABASE_URL=postgres://postgres@localhost:5432/be
